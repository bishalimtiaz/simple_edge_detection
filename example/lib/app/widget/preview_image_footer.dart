import 'package:flutter/material.dart';

class PreviewImageFooter extends StatelessWidget {
  const PreviewImageFooter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
